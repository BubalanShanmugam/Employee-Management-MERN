# Backend environment example
# Copy this file to `.env` and fill real values before running the app

# Postgres connection URL (format: postgres://user:password@host:port/dbname)
DATABASE_URL=postgres://employee_user:SANTHIYA2210@127.0.0.1:5432/employee_db

# JWT secret used to sign tokens
JWT_SECRET=fHY+2GdEszlZrfxgIWewTB6v5Uw2fRfRwdUorIeL8cqiBFF4mX59i8HszpWTAKpk

# Server port
PORT=3000

# bcrypt salt rounds (optional)
BCRYPT_SALT_ROUNDS=10

# Sample SQL query
SQL_QUERY=SELECT id, name, email, role, employee_id, department FROM users LIMIT 10;
